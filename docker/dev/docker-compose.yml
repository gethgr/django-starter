services:
  db:
    image: postgres:16-alpine
    container_name: django_dev_db
    env_file:
      - ../../.env.dev
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped

volumes:
  postgres_dev_data:
